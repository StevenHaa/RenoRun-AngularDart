class Order { 
  String email;
  String colour;
  int productId;

  Order(String email, String colour, int productId) { 
    this.email = email;
    this.colour = colour;
    this.productId = productId;
  }
}